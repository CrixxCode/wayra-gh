DJANGO_SECRET_KEY=replace-with-long-random-secret
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=api.example.com
DJANGO_TIME_ZONE=America/Bogota

# DB engine: sqlite|postgres
DB_ENGINE=postgres
DB_NAME=gestion_hotelera
DB_USER=postgres
DB_PASSWORD=replace-db-password
DB_HOST=127.0.0.1
DB_PORT=5432

# CORS / CSRF for frontend
CORS_ALLOW_ALL_ORIGINS=False
CORS_ALLOWED_ORIGINS=https://app.example.com
CSRF_TRUSTED_ORIGINS=https://app.example.com

# Session / CSRF cookies
SESSION_COOKIE_SECURE=True
CSRF_COOKIE_SECURE=True
SESSION_COOKIE_SAMESITE=Lax
SESSION_COOKIE_AGE=604800

# HTTPS hardening
SECURE_SSL_REDIRECT=True
SECURE_HSTS_SECONDS=31536000
SECURE_HSTS_INCLUDE_SUBDOMAINS=True
SECURE_HSTS_PRELOAD=False
SECURE_PROXY_SSL_HEADER_ENABLED=True
USE_X_FORWARDED_HOST=True
SECURE_REFERRER_POLICY=same-origin

# Static files
DJANGO_STATIC_ROOT=staticfiles

# Email: SMTP only works on Railway Pro or outside restricted SMTP networks.
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=notifications@example.com
EMAIL_HOST_PASSWORD=replace-email-password
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=notifications@example.com
SERVER_EMAIL=notifications@example.com
EMAIL_TIMEOUT=20

# Email alternative for Railway Free/Trial/Hobby: HTTPS API via Resend.
# EMAIL_BACKEND=anymail.backends.resend.EmailBackend
# RESEND_API_KEY=re_replace-with-resend-api-key
# DEFAULT_FROM_EMAIL=Wayra <onboarding@resend.dev>
# SERVER_EMAIL=Wayra <onboarding@resend.dev>

PASSWORD_RESET_COOKIE_MAX_AGE=1800
ALLOW_PUBLIC_USER_REGISTRATION=False
ALLOW_PUBLIC_CLIENT_REGISTRATION=False
